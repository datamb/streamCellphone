<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <!-- Botão para selecionar o vídeo -->
    <Button
        android:id="@+id/buttonSelectVideo"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Selecionar Vídeo"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="40dp" />

    <!-- TextView para exibir o caminho do vídeo selecionado -->
    <TextView
        android:id="@+id/textViewVideoPath"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Nenhum vídeo selecionado"
        app:layout_constraintTop_toBottomOf="@id/buttonSelectVideo"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="20dp" />

    <!-- Botão para iniciar a transmissão -->
    <Button
        android:id="@+id/buttonStartStreaming"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Iniciar Transmissão"
        app:layout_constraintTop_toBottomOf="@id/textViewVideoPath"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="20dp"
        android:enabled="false" /> <!-- Desativado até o vídeo ser selecionado -->

</androidx.constraintlayout.widget.ConstraintLayout>

